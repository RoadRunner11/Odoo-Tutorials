<odoo>
  <data>
    <!-- explicit list view definition -->

    <record model="ir.ui.view" id="sale_bulk_cancel_action">
      <field name="name">Cancel These Orders</field>
      <field name="model">sale.order</field>
      <field name="inherit_id" ref="sale.view_order_form" />
      <field name="priority">10</field>
      <field name="arch" type="xml">
        <form>
          
          <footer>
              <button name="update_state" string="Update" type="object"/>
              <button string="Cancel" class="btn-secondary" special="cancel"/>
          </footer>
        </form>
      </field>
    </record>


    <!-- actions opening views on models -->

    <!-- <record model="ir.actions.act_window" id="sale_bulk_cancel.action_window">
      <field name="name">Cancel Orders</field>
      <field name="res_model">sale.order</field>
      <field name="type">ir.actions.act_window</field>
      <field name="view_mode">form</field>
      <field name="domain">[('state', '!=', 'cancel')]</field>
      <field name="context">{}</field>
    </record> -->


    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="sale_bulk_cancel.action_server">
      <field name="name">sale_bulk_cancel server</field>
      <field name="model_id" ref="model_sale_bulk_cancel_sale_bulk_cancel"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="sale_bulk_cancel" id="sale_bulk_cancel.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="sale_bulk_cancel.menu_1" parent="sale_bulk_cancel.menu_root"/>
    <menuitem name="Menu 2" id="sale_bulk_cancel.menu_2" parent="sale_bulk_cancel.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="sale_bulk_cancel.menu_1_list" parent="sale_bulk_cancel.menu_1"
              action="sale_bulk_cancel.action_window"/>
    <menuitem name="Server to list" id="sale_bulk_cancel" parent="sale_bulk_cancel.menu_2"
              action="sale_bulk_cancel.action_server"/>
-->

  <act_window name="Cancel Orders"
          id="sale_bulk_cancel_action"
          res_model="sale.order"
          binding_model="sale.order"
          binding_views="list"
          view_mode="form"
          target="new"
      />
  </data>
</odoo>
